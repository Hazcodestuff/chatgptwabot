build:
  hello-world:
     image: okteto.dev/hello-world:latest
     context: .
     dockerfile: Dockerfile
deploy:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
